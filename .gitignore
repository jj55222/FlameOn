# Environment
.env
*.pyc
__pycache__/
.venv/
venv/

# Google API credentials (never commit these)
gdoc_token.json
gdoc_credentials.json
gdoc_state.json

# Brave API billing state (machine-specific, auto-generated)
brave_quota.json

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Output artifacts (large files, regeneratable)
*.wav
*.mp3
*.mp4
*.webm

# Claude Code local settings
.claude/settings.local.json

# P6 documentary staging — never commit media or scratch
.tmp/
*.mp4
*.mov
*.mkv
*.zip

# Oversized case-evidence (exceeds GitHub's 100MB file limit — use the manual-copy
# bucket, see MIGRATION.md). This 422MB PDF was stripped from history on 2026-06-26.
pipeline3_audio/case_0409-18/Production - 0409-18.pdf
